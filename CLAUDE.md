# CLAUDE.md

Use [AGENTS.md](AGENTS.md) as the canonical project instructions for coding agents.

For download setup, the PO Token provider, output files, and M4A artwork handling, follow its Common Commands, Data And Output Layout, Download Logic, and Postprocessing Notes.

Claude-specific note: keep answers and code changes aligned with `AGENTS.md`, and update `AGENTS.md` in the same change whenever behavior, commands, endpoints, file layout, Docker/runtime config, dependency requirements, tests, or important caveats change.
