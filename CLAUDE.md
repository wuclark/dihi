# CLAUDE.md

Use `AGENTS.md` as the canonical project instructions for coding agents.

Claude-specific note: keep answers and code changes aligned with `AGENTS.md`, and update `AGENTS.md` in the same change whenever behavior, commands, endpoints, file layout, Docker/runtime config, dependency requirements, tests, or important caveats change.
